{
  "app": "npx tsx bin/cdk.ts",
  "context": {
    "environments": {
      "dev": {
        "account": "${AWS_ACCOUNT_ID}",
        "region": "eu-central-1",
        "environmentName": "dev",
        "subdomain": "parkrun-dev"
      },
      "prod": {
        "account": "${AWS_ACCOUNT_ID}",
        "region": "eu-central-1",
        "environmentName": "prod",
        "subdomain": "parkrun"
      }
    },
    "accountId": "${AWS_ACCOUNT_ID}",
    "hostedZoneId": "${HOSTED_ZONE_ID}",
    "domainName": "${DOMAIN_NAME}"
  }
}
