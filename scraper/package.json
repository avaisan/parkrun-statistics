{
  "name": "@parkrun/scraper",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "scraper": "prisma generate --schema=../prisma/schema.prisma && tsx src/index.ts",
    "lint": "eslint .",
    "clean": "rm -rf dist build function.zip"
  },
  "dependencies": {
    "@parkrun/prisma": "*",
    "@prisma/client": "^7.2.0",
    "axios": "^1.12.2",
    "cheerio": "^1.1.2"
  },
  "devDependencies": {}
}
